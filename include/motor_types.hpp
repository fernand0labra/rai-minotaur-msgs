#ifndef MOTOR_TYPES_HPP_
#define MOTOR_TYPES_HPP_

enum motorType {
    BAM_PAD,
    FAM_PAD,
    PUSHER_BPM,
    PUSHER_DPM,
    AUGER_PIPE
};

enum motorPos {
    LEFT = 10,
    RIGHT = 11,
    TOP = 12
};

#endif